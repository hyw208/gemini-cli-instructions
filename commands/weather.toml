description = "Get weather information for a city"
prompt = """
Get current weather or forecast for a city:

Available options:
- Current weather: --city "CITY"
- Forecast: --city "CITY" --forecast [DAYS]

Examples:
- Current weather in Beijing: --city "Beijing"
- 7-day forecast for London: --city "London" --forecast 7

!{ PYTHONPATH=./commands ./commands/.venv/bin/python -m scripts.weather {{args}}}
"""
